import React from "react";

function Home({ UserID: _UserID }) {
	//define {userBoards, _removeBoard } with weird js object destructuring bullshit

	return (
		<div>
			<h1>Home</h1>
		</div>
	);
}

export default Home;
